  radixSortLSD_keys(A:[] int(64)) -- (indices)
 radixSortLSD_ranks(A:[] int(64)) -- (indices)
  radixSortLSD_keys(A:[] int(64)) -- (rev indices)
 radixSortLSD_ranks(A:[] int(64)) -- (rev indices)
  radixSortLSD_keys(A:[] int(64)) -- (rev shifted indices)
 radixSortLSD_ranks(A:[] int(64)) -- (rev shifted indices)
  radixSortLSD_keys(A:[] int(64)) -- (rev max-indices)
 radixSortLSD_ranks(A:[] int(64)) -- (rev max-indices)
  radixSortLSD_keys(A:[] int(64)) -- (negate even indices)
 radixSortLSD_ranks(A:[] int(64)) -- (negate even indices)
 radixSortLSD_keys(A:[] real(64)) -- (indices)
radixSortLSD_ranks(A:[] real(64)) -- (indices)
 radixSortLSD_keys(A:[] real(64)) -- (rev indices)
radixSortLSD_ranks(A:[] real(64)) -- (rev indices)
 radixSortLSD_keys(A:[] real(64)) -- (rev shifted indices)
radixSortLSD_ranks(A:[] real(64)) -- (rev shifted indices)
 radixSortLSD_keys(A:[] real(64)) -- (rev max-indices)
radixSortLSD_ranks(A:[] real(64)) -- (rev max-indices)
 radixSortLSD_keys(A:[] real(64)) -- (negate even indices)
radixSortLSD_ranks(A:[] real(64)) -- (negate even indices)
  radixSortLSD_keys(A:[] int(64)) -- (rand int(8) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand int(8) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand int(16) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand int(16) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand int(32) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand int(32) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand int(64) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand int(64) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand uint(8) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand uint(8) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand uint(16) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand uint(16) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand uint(32) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand uint(32) vals)
  radixSortLSD_keys(A:[] int(64)) -- (rand uint(64) vals)
 radixSortLSD_ranks(A:[] int(64)) -- (rand uint(64) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand int(8) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand int(8) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand int(16) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand int(16) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand int(32) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand int(32) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand int(64) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand int(64) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand uint(8) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand uint(8) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand uint(16) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand uint(16) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand uint(32) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand uint(32) vals)
 radixSortLSD_keys(A:[] real(64)) -- (rand uint(64) vals)
radixSortLSD_ranks(A:[] real(64)) -- (rand uint(64) vals)
