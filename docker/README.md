This folder contains sources for building Docker images for the project. The
subdirectories are used to delineate different images for different purposes. 
Each subdirectory contains a Dockerfile and any additional files needed to build/use the image.